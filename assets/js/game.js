// Complete JavaScript game logic from the previous version
class CppCodeQuest {
    constructor() {
        this.currentLevel = 0;
        this.inventory = [];
        this.progress = 0;
        this.init();
    }
    
    // ... (all the game logic from the previous version)
}

// Initialize the game
const game = new CppCodeQuest();